// Punkt wejścia dla przyszłego serwera (cron + monitoring pozycji).
// W obecnym etapie MVP screener uruchamiany jest ręcznie: `npm run screener:run`.
console.log(
  "Backend AI Stock Advisor — na tym etapie MVP nie ma jeszcze serwera/crona. " +
    "Użyj `npm run screener:run`, żeby odpalić screener."
);
